= simple_form_for(@map) do |f|
  .grid-x
    .large-12.cell
      = f.error_notification
  .grid-x
    .large-12.cell
      .form-inputs
        = f.input :title
        = f.input :text
        = f.input :published
        = f.association :group, collection: Group.by_user(current_user), include_blank: false
      .form-actions
        %p
          = f.button :submit, :class => "button"
- if admin?
  - if @map.layers.count == 0
    %hr
    = link_to @map, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'button warning' do
      %i.fi.fi-x
      Destroy
  %p.shy Only admins can delete a map. But: You can destroy this map only if you've deleted all places and layers before :#