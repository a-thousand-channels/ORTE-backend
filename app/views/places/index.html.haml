
#table-wrapper
  = link_to map_layer_url(@map,@layer), :class=>"close-button button small", "aria-label"=>"Close back",:type=>"button" do
    %span{"aria-hidden"=>"true"}
      %i.fi.fi-x
  %h3
    = link_to map_path(@layer.map) do
      = @layer.map.title
    \>
    = link_to map_layer_path(@layer.map,@layer) do
      = @layer.title
    (#{@places.count})


  %table
    %thead
      %tr
        %th.show-for-large Date
        %th Place
        %th.show-for-large
          %i.fi-eye.fi-24
        %th

    %tbody
      - @places.order(:startdate).each do |place|
        %tr
          %td.show-for-medium
            = smart_date_display(place.startdate, place.enddate)
          %td
            %p.show-for-small-only
              = smart_date_display(place.startdate, place.enddate)

            %h4
              = link_to map_layer_place_path(place.layer.map,place.layer,place) do
                = place.title
            %p
              = link_to place.link do
                = place.link
            %p
              = place.location
              %br
              = place.address
              %br
              = place.zip
              = place.city
            %p.hint.text-left
              - if place.lat
                #{place.lat[0..6]}/#{place.lon[0..5]}
          %td.show-for-large
            - if place.published
              %i.fi-check.fi-24
            - else
              %i.fi-lock.fi-24
          %td
            = link_to edit_map_layer_place_path(place.layer.map,place.layer,place), :class => "button" do
              %i.fi-pencil.fi-24
              Edit
            %br
            - if place.images.count > 0
              = link_to images_path(place_id: place.id), :class => "button" do
                %i.fi-photo.fi-24
                Images
            - else
              = link_to new_image_path(place_id: place.id), :class => "button" do
                %i.fi-plus.fi-24
                Add&nbsp;image
            %br
            = link_to map_layer_place_path(place.layer.map,place.layer,place), method: :delete, data: { confirm: 'Are you sure?' }, :class => "button warning" do
              %i.fi-trash.fi-24
              Destroy
